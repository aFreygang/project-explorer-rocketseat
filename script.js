let mudar_base_cor = document.documentElement
var cores = [
  'light-theme-blue',
  'light-theme-pink',
  'dark-theme-blue',
  'dark-theme-pink'
]
let listas_cores = document.querySelectorAll('.alterar-cores')

listas_cores.addEventListener('mousemove', {
  cores.forEach(cor => {
    if (condition) {
      
    }
  })
})
